

# Playwright MCP scratch output from local verification runs
.playwright-mcp/

# Never commit environment files or private keys. This site needs no secrets at
# build time, so anything matching these belongs in GitHub Secrets instead.
.env
.env.*
!.env.example
*.pem
*.pfx
*.key
